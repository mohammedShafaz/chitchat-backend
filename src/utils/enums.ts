export enum FriendRequestStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected'
}

export enum MessageStatus {
    SEND = 'send',
    DELIVERED = 'delivered',
    SEEN = 'seen'
}

export enum MessageType{
    TEXT='text',
    IMAGE='image',
    VIDEO='video'
}